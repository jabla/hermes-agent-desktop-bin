diff --git a/apps/desktop/src/store/voice-prefs.test.ts b/apps/desktop/src/store/voice-prefs.test.ts
index 3cf9c7e..cec75d5 100644
--- a/apps/desktop/src/store/voice-prefs.test.ts
+++ b/apps/desktop/src/store/voice-prefs.test.ts
@@ -16,13 +16,17 @@ import {
   applyVoiceStopPhraseFromConfig
 } from './voice-prefs'
 
+// jsdom methods live on the prototype; the Node fallback has own methods.
+const storageMethods =
+  Object.hasOwn(localStorage, 'setItem') ? localStorage : Object.getPrototypeOf(localStorage)
+
 it('keeps the desktop toggle local across config refreshes', async () => {
   for (const fails of [false, true]) {
     for (const enabled of [false, true]) {
       localStorage.clear()
       vi.resetModules()
       const prefs = await import('./voice-prefs')
-      const write = vi.spyOn(localStorage, 'setItem')
+      const write = vi.spyOn(storageMethods, 'setItem')
 
       if (fails) {
         write.mockImplementation(() => {
@@ -51,7 +55,7 @@ it('migrates the legacy preference once, not on every refresh', async () => {
       localStorage.clear()
       vi.resetModules()
       const prefs = await import('./voice-prefs')
-      const write = vi.spyOn(localStorage, 'setItem')
+      const write = vi.spyOn(storageMethods, 'setItem')
 
       if (fails) {
         write.mockImplementation(() => {
